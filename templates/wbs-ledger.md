# WBS 任务台账 — [项目名称]

> 此台账是项目的单一事实来源（Single Source of Truth）。所有任务状态、证据、变更记录都必须在此更新。

## 共享上下文（Shared Context）

> 所有任务的共同背景放在这里，避免每个 Context Brief 重复编写。各任务在 Context Brief 中引用即可。

- **项目类型**: e.g. Node.js 18 + Express + PostgreSQL
- **代码规范**: e.g. ESLint + Prettier, 2-space indent
- **技术前提**: e.g. Docker, pnpm 9, Node 20+
- **已知架构决策**: e.g. 使用 JWT 认证，Redis 做缓存
- **外部依赖**: e.g. MySQL 8, Redis 7, Google Cloud SDK

## 环境前提

> 子代理执行前应校验的环境条件，避免因环境不一致导致无效执行。

- **操作系统**: e.g. macOS 14 / Ubuntu 22.04
- **运行时版本**: e.g. Node 20+, Python 3.11+
- **磁盘空间**: e.g. 至少 5GB 可用
- **网络要求**: e.g. 需要访问 npm registry / GitHub
- **权限要求**: e.g. 需要 sudo 权限 / Docker 访问
- **已安装工具**: e.g. git, ffmpeg, docker

## 任务摘要

- **任务名称**: [一句话描述]
- **预期成果**: [完成后看起来是什么样的]
- **成功标准**: [如何验证完成]
- **约束条件**: [时间/资源/技术限制]
- **外部依赖**: [需要外部团队/服务/资源]
- **已知风险**: [可能阻碍进度的因素]

## WBS 任务分解

| ID | 任务名称 | 依赖 | Context Brief | 退出标准 | 证据 | 状态 |
|----|---------|------|--------------|---------|------|------|
| 1  | [任务名] | - | [冷启动上下文] | [如何知道完成] | [验证命令输出] | todo |

### Context Brief 编写规范

每个任务的 Context Brief 必须自包含，新 agent 无需读前置任务即可冷启动执行：

1. **本任务目标**：一句话说明做什么
2. **前置产物**：依赖任务完成后产出了什么
3. **涉及文件**：需要创建/修改的精确文件路径
4. **关键约束**：架构决策、命名规范、技术选型
5. **验收要点**：从 exit_criteria 中摘取最关键的 1-2 条

### 允许的状态

| 状态 | 含义 |
|------|------|
| `todo` | 未开始 |
| `doing` | 执行中 |
| `done` | 已完成（必须有证据） |
| `blocked` | 阻塞（必须说明原因） |
| `skipped` | 跳过（必须说明原因） |

## 计划变更记录（Mutation Log）

> 铁律：所有计划变更必须记录，不删除原任务行（用 skipped 标记）。

| 时间 | 变更类型 | 影响任务 | 原因 | 新任务 |
|------|---------|---------|------|--------|
| | split / insert / skip / reorder / abandon | | | |

### 允许的突变类型

| 类型 | 说明 | 规则 |
|------|------|------|
| **split** | 拆分任务为更小单元 | 原任务标记 skipped，子任务用 X.1, X.2 编号 |
| **insert** | 在已有任务之间插入新任务 | 新任务用小数字编号（如 2.5） |
| **skip** | 跳过不需要的任务 | 标记 skipped + 注明原因 |
| **reorder** | 重排任务顺序 | 更新 Dependencies 列，验证无循环依赖 |
| **abandon** | 废弃任务（方向错误） | 标记 skipped + 注明"废弃：原因"，不移除行 |

## 当前执行状态（Active State）

- **当前任务**: 
- **最后完成**: 
- **最后检查点**: 
- **从此处恢复**: 
- **当前阻塞**: 

## 心跳日志（Heartbeat Log）

> 每个子任务完成后更新，记录执行进度。

| 时间 | 活跃任务 | 已完成 | 证据 | 恢复点 |
|------|---------|--------|------|--------|
| | | | | |

## 交付总结（Delivery Summary）

- **已完成工作**: 
- **证据包**: 
- **剩余阻塞/跳过**: 
- **残留风险**: 
- **最终交接说明**: 
